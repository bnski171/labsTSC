function analyzeData(numbers: number[], str: string): [number, number, number, string] {
    const maxNumber = Math.max(...numbers);
    const minNumber = Math.min(...numbers);
    const sum = numbers.reduce((acc, val)) => acc + vall
}